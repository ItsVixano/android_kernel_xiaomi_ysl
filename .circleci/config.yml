version: 2.1
jobs:
  compile:
   docker:
      - image: fr3akyphantom/droid-builder:latest
   steps:
      - run:
          command: |
           git clone https://github.com/ItsVixano/android_kernel_xiaomi_ysl --single-branch -b twrp --depth=1 ysl
           cd ysl
           wget https://raw.githubusercontent.com/ItsVixano/personalscripts/master/kernel/twrp-ysl.sh
           bash twrp-ysl.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
